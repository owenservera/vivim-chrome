{
  "name": "vivim-extension",
  "version": "2.0.0",
  "description": "VIVIM Extension v2.0 — Multi-Provider AI Assistant",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "clean": "rm -rf dist",
    "test": "node test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}