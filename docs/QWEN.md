## Qwen Added Memories
- ChatGPT streaming to sidepanel is 100% working for chatgpt.com and chat.com. The working codebase is in C:\0-BlackBoxProject-0\vivim-app-og\vivim-app\extensions\chrome\poc\. Key working files: inject-web.js (delta encoding v1 parser), content.js (bridge relay), background.js (message router + storage), sidepanel.js (UI + tab tracking), manifest.json. The streaming uses SSE delta encoding with JSON patch operations (op: "append", path: "/message/content/parts/0").
- Architecture refactoring note: The data flow between inject-web.js → content.js → background.js → sidepanel.js needs a coordinator/operator to ensure smooth sailing. Currently each layer fires async messages without coordination, causing race conditions. The proposed multi-provider architecture (ARCHITECTURE-REFACTOR.md) should include a StreamPipeline class that normalizes, deduplicates, and fans-out messages with proper sequencing guarantees.
